h2 style=mailer_style(:h2)
  == t('mailers.sentinel_mailer.finished.body', referential_name: @referential.name)
  br
  = mailer_button(t('mailers.sentinel_mailer.finished.button'), referential_url(@referential))
