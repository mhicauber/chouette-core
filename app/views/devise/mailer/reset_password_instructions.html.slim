h2 style=mailer_style(:h2)
  == t('mailers.password_mailer.updated.body')
  br
  = mailer_button(t('mailers.password_mailer.updated.button'), edit_password_url(@user, :reset_password_token => @token))
