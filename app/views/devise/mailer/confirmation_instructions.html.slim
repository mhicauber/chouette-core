h2 style=mailer_style(:h2)
  == t('mailers.confirmation_mailer.body', email: @user.email).html_safe
  br
  = mailer_button(t('mailers.confirmation_mailer.button'), confirmation_url(@user, :confirmation_token => @token))
