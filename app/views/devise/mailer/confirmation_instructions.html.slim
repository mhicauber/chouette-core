h2 style=mailer_style(:h2)
  == t('mailers.confirmation_mailer.title', email: @user.email)

p== t('mailers.confirmation_mailer.body')
p= mailer_button(t('mailers.confirmation_mailer.button'), confirmation_url(@user, :confirmation_token => @token))
