en:
  stop_area_referentials:
    actions:
      sync: "Launch a new synchronization"
      cancel_sync: "Cancel synchronization"
    show:
      title: 'Stop area referential'
      synchronized: Synchronized
      status: status
      message: Message
  activerecord:
    models:
      stop_area_referential:
        one: "stop area referential"
        other: "stop area referentials"
